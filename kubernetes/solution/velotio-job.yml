apiVersion: batch/v1
kind: Job
metadata:
  namespace: velotio-ajinkya
  name: velotio-job
spec:
  completions: 5
  template:
    spec:
      containers:
      - name: busybox
        image: busybox
        command: ["/bin/sh", "-c", "echo hello; sleep 30; echo world"]
      restartPolicy: OnFailure